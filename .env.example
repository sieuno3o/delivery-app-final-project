# Local PostgreSQL connection used by Docker Compose.
DATABASE_URL=postgresql://delivery:delivery@localhost:5433/delivery

# Comma-separated emails allowed to access the future admin screen.
ADMIN_EMAILS=admin@example.com
