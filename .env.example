# Local PostgreSQL connection. Docker configuration will be added in the next task.
DATABASE_URL=postgresql://delivery:delivery@localhost:5432/delivery

# Replace this with a long random value in .env.local and Vercel.
SESSION_SECRET=replace-with-a-long-random-value

# Comma-separated emails allowed to access the future admin screen.
ADMIN_EMAILS=admin@example.com
